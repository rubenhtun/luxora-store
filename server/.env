# The port number your Node.js server will listen on
PORT=3000

# MongoDB connection string
# This connects to a local MongoDB instance and uses the database named 'luxora-store'
MONGODB_URI=mongodb://127.0.0.1:27017/luxora-store

# Secret key used to sign JWT tokens for authentication
# Keep this secret and never commit to public repositories
JWT_SECRET=superSecretKeyForLuxora123!@#
